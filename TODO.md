

# Merge Interactive TUI functions into a single function
RunInteractiveChanges()
RunInteractiveSpecs()
RunInteractiveAll()
RunInteractiveArchive()
should all be merged into one function, `RunInteractive(items)`
