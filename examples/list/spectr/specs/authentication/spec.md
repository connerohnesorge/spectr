# Authentication

## Requirements

### Requirement: Basic Login

The system SHALL allow users to login with email and password.

#### Scenario: Valid credentials

- **WHEN** user provides valid credentials
- **THEN** system creates a session
